<style>
    .spacer {
        height: 0;
    }

    .wrapper {
        height: 150vh;
        position: relative;
        top: -100px;
    }
    .panel {
        position: sticky;
        top: 50px;
        color: rgb(0, 0, 0);
    }

    .contenido {
        height: 200vh;
        padding: 40px;
        position: absolute;
        width: 100%;
        border: 1px solid red;
    }

    .contenido2 {
        height: 200vh;
        padding: 40px;
        width: 100%;
    }

    p {
        font-family: "Open Sans", sans-serif;
        font-size: 8rem;
        animation: reveal linear both;
        animation-timeline: view();
        animation-range: entry 50% cover 50%;
        font-weight: bold;
        animation-delay: calc(sibling-index() * 1s);
        line-height: 0.9;
        text-align: center;
        letter-spacing: -6px;
    }

    @keyframes reveal {
        0% {
            transform: translateY(500px);
            opacity: 0;
        }
        90% {
            opacity: 0;
        }
        98% {
            transform: translateY(-5px);
            opacity: 1;
        }
        100% {
            transform: translateY(0);
            opacity: 1;
        }
    }

    /* 📱 MEDIA QUERY para pantallas más pequeñas (Móviles y Tablets) */
    @media (max-width: 768px) {
        p {
            font-size: 3rem; /* Tamaño reducido para móviles */
            line-height: 1.2; /* Aumentar la altura de línea para mejor legibilidad */
            letter-spacing: -2px; /* Reducir el espaciado entre letras */
            padding: 0 10px; /* Añadir un poco de padding horizontal */
        }

        .wrapper {
            /* Ajustar si es necesario, puede que en móviles necesites menos altura */
            height: 100vh;
            top: 0;
        }

        .panel {
            /* Asegurar que el contenido se centre y tenga margen */
            top: 20px;
            padding: 0 5px;
        }
    }
</style>

<section>
    <section class="spacer"></section>

    <section class="wrapper">
        <div class="panel">
            <p>APIs, SQL & Performance</p>
            <p>UX & Business Focused</p>
            <p>Software Developer</p>
            <p>End-to-End Apps</p>
            <p>Based in Spain</p>
        </div>
        <!-- <section class="contenido">
            <h1>Contenido largo...</h1>
        </section> -->
    </section>
    <!-- <section class="contenido2">
        <h1>Contenido largo...</h1>
    </section> -->
</section>
